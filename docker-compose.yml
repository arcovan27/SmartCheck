services:
  postgres:
    image: postgres:16-alpine
    container_name: smartcheck-postgres
    environment:
      POSTGRES_DB: smartcheck
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 20

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    container_name: smartcheck-api
    environment:
      PORT: 3333
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/smartcheck
      JWT_SECRET: supersecretkey
      WEB_ORIGIN: http://localhost:5173
      UPLOAD_DIR: uploads
      RUN_SEED_ON_START: "false"
    volumes:
      - ./apps/api/uploads:/app/apps/api/uploads
      - ./apps/api:/app/apps/api
      - ./package.json:/app/package.json
      - /app/node_modules
    ports:
      - "3333:3333"
    depends_on:
      postgres:
        condition: service_healthy

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    container_name: smartcheck-web
    environment:
      VITE_API_URL: http://localhost:3333
    volumes:
      - ./apps/web:/app/apps/web
      - ./package.json:/app/package.json
      - /app/node_modules
    ports:
      - "5173:5173"
    depends_on:
      - api

volumes:
  postgres_data:
    name: smartcheck_postgres_data
